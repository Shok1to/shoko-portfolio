import type React from "react"
import "@/app/globals.css"
import { Inter } from "next/font/google"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata = {
  title: "Shoko Developer | Fullstack Web Developer Portfolio",
  description:
    "Portfolio of Shoko, a fullstack web developer specializing in React, Next.js, and Node.js with 5+ years of experience building web applications.",
  keywords:
    "fullstack developer, web developer, React, Next.js, Node.js, portfolio",
  generator: "v0.dev",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}

import "./globals.css"
